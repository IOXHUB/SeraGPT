import BlogPostPage from '../[slug]/page';

export const dynamic = 'force-dynamic';

export default function SeraMimarligiTasarim() {
  return <BlogPostPage params={{ slug: 'sera-mimarligi-tasarim' }} />;
}
