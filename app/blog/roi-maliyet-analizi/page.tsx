import BlogPostPage from '../[slug]/page';

export const dynamic = 'force-dynamic';

export default function ROIMaliyetAnalizi() {
  return <BlogPostPage params={{ slug: 'roi-maliyet-analizi' }} />;
}
